# K?t n?i t?i MySQL local
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate config
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.docker.compose.enabled=false
# Ch?n dialect chu?n cho MySQL
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# C?u h?nh cho Azure Blob Storage
#spring.cloud.azure.storage.blob.connection-string=DefaultEndpointsProtocol=https;AccountName=blobcontainerpbl5;AccountKey=;EndpointSuffix=core.windows.net
#spring.cloud.azure.storage.blob.container-name=blobcontainerpbl5